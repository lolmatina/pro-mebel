RewriteEngine On
RewriteBase /api/

# Serve uploads directly from /uploads
RewriteRule ^uploads/(.*)$ uploads/$1 [L]

# Front controller for everything else
RewriteCond %{REQUEST_FILENAME} !-f
RewriteCond %{REQUEST_FILENAME} !-d
RewriteRule ^ index.php [QSA,L]